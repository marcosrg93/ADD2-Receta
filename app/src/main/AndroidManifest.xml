<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rubino.add2sqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".actividades.Alta">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Principal"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".InfoReceta"
            android:label="@string/title_activity_info_receta"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".actividades.AltaIngrediente"
            android:label="@string/title_activity_info_receta"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".actividades.EditarReceta"></activity>

        <activity
            android:name=".actividades.Lista"
            android:label="@string/title_activity_info_receta"
            android:theme="@style/AppTheme" />
    </application>

</manifest>
